import { RepoLanguageDirective } from './repo-language.directive';

describe('RepoLanguageDirective', () => {
  it('should create an instance', () => {
    const directive = new RepoLanguageDirective();
    expect(directive).toBeTruthy();
  });
});
